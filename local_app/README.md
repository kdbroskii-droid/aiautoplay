# Local test app

Run this window with:

```bash
python3 -m local_app.app
```

It is a self-contained user-owned test environment. The AI controls the simulated player through the application's own Python API; it does not inject keyboard or mouse events into other applications.

Press **Esc** to close it.
