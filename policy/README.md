# Policy

The policy layer decides which abstract action the agent should take.

Components:
- Policy interface
- Epsilon-greedy exploration
- Action cooldown tracking
- Environment-aware action filtering

The policy operates on abstract actions and does not inject input into public games.
