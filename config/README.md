# Configuration

Project-wide configuration belongs here.

The runtime agent configuration is currently in `agent/config.json`.
Keep secrets, tokens, credentials, and machine-specific paths out of Git.
