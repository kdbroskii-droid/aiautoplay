# Chromebook

The project is designed for the ChromeOS Linux (Crostini) environment.

## Recommended runtime

Use Python 3.10+ and the standard library runtime first. Heavy ML dependencies can be added later when the custom environment actually needs them.

## Run

From the repository root:

```bash
./run.sh
```
