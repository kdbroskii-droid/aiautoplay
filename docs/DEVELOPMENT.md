# Development

Prefer small, testable Python modules and deterministic behavior where possible.

Run validation with:

```bash
python3 -m tools.validate
```

Run tests with:

```bash
python3 -m pytest -q
```
