# Training

The training layer connects the policy, learner, rewards, and replay memory.

`Trainer` provides a small deterministic coordinator suitable for the Chromebook sandbox.
It records experiences and metrics without requiring a heavyweight ML framework.
